name: release-lazy-package

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag. Leave empty to use v<CMake project version>.'
        required: false
        type: string
      prerelease:
        description: 'Publish as prerelease'
        required: true
        default: false
        type: boolean
      draft:
        description: 'Keep as Draft (not public / not Latest). Normally leave OFF.'
        required: true
        default: false
        type: boolean

permissions:
  contents: write

concurrency:
  group: ai-audio-analyzer-release
  cancel-in-progress: false

jobs:
  validate:
    name: Validate release sources
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: mcp/requirements.txt

      - name: Validate scripts, docs and MCP source
        shell: bash
        run: |
          set -euo pipefail
          bash -n release/macos/install.sh
          bash -n release/macos/Install.command
          test -s release/windows/Install.ps1
          test -s release/windows/Install.cmd
          test -s release/common/START-HERE.md
          test -s release/common/MCP-SETUP.md
          test -s release/common/INSTALL.en.md
          test -s release/common/INSTALL.zh-CN.md
          test -s skills/ai-analyzer-flstudio/SKILL.md
          test -s skills/ai-analyzer-flstudio/references/analyzer-mcp.md
          test -s skills/ai-analyzer-flstudio/references/song-memory.md
          test -s skills/ai-analyzer-flstudio/references/mono-compatibility.md
          test -s skills/ai-analyzer-flstudio/references/reference-comparison.md
          test -s LICENSE

          python -m pip install --upgrade pip
          python -m pip install -r mcp/requirements.txt

          python -m py_compile \
            mcp/server.py \
            mcp/analyzer_core.py \
            mcp/self_description.py \
            mcp/project_tools.py \
            mcp/project_identity_tools.py \
            mcp/temporal_tools.py \
            mcp/masking_tools.py \
            mcp/stereo_tools.py \
            mcp/semantic_tools.py \
            mcp/performance_tools.py \
            mcp/control_tools.py \
            mcp/song_tools.py \
            mcp/historical_detail_store.py \
            mcp/historical_detail_profile.py \
            mcp/historical_detail_pair.py \
            mcp/historical_detail_tools.py \
            mcp/section_tools.py \
            mcp/track_story_tools.py \
            mcp/section_relationship_tools.py \
            mcp/verification_tools.py \
            mcp/range_tools.py \
            mcp/range_verification_tools.py \
            mcp/dynamics_tools.py \
            mcp/mono_compatibility_tools.py \
            mcp/reference_tools.py \
            mcp/ci_regression.py \
            mcp/relationship_regression.py \
            mcp/range_verification_regression.py \
            mcp/p4b_regression.py \
            mcp/dynamics_regression.py \
            mcp/mono_compatibility_regression.py \
            mcp/reference_regression.py

          AI_ANALYZER_SELF_TEST=1 python mcp/server.py
          python mcp/ci_regression.py
          python mcp/relationship_regression.py
          python mcp/range_verification_regression.py
          python mcp/p4b_regression.py
          python mcp/dynamics_regression.py
          python mcp/mono_compatibility_regression.py
          python mcp/reference_regression.py

  mcp-runtime:
    name: MCP runtime (${{ matrix.label }})
    needs: validate
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            label: windows-x64
            artifact: mcp-runtime-windows-x64
          - os: macos-latest
            label: macos-arm64
            artifact: mcp-runtime-macos-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: mcp/requirements.txt

      - name: Verify macOS runtime architecture
        if: runner.os == 'macOS'
        shell: bash
        run: test "$(uname -m)" = "arm64"

      - name: Install MCP build dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r mcp/requirements.txt
          python -m pip install 'pyinstaller>=6.10,<7'

      - name: Validate source MCP 1.2
        shell: bash
        run: AI_ANALYZER_SELF_TEST=1 python mcp/server.py

      - name: Build standalone MCP runtime
        shell: bash
        run: |
          python -m PyInstaller \
            --noconfirm \
            --clean \
            -F \
            --name ai-audio-analyzer-mcp \
            --paths mcp \
            --collect-all mcp \
            --collect-all pythonosc \
            mcp/server.py

      - name: Smoke-test packaged runtime on macOS
        if: runner.os == 'macOS'
        shell: bash
        run: |
          set -euo pipefail
          chmod +x dist/ai-audio-analyzer-mcp
          AI_ANALYZER_SELF_TEST=1 dist/ai-audio-analyzer-mcp
          file dist/ai-audio-analyzer-mcp

      - name: Smoke-test packaged runtime on Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $env:AI_ANALYZER_SELF_TEST = '1'
          & 'dist/ai-audio-analyzer-mcp.exe'
          if ($LASTEXITCODE -ne 0) { throw "Packaged MCP self-test failed: $LASTEXITCODE" }
          Remove-Item Env:AI_ANALYZER_SELF_TEST -ErrorAction SilentlyContinue

      - name: Stage runtime file
        shell: bash
        run: |
          set -euo pipefail
          rm -rf runtime-artifact
          mkdir -p runtime-artifact
          if [ "${{ runner.os }}" = "Windows" ]; then
            cp dist/ai-audio-analyzer-mcp.exe runtime-artifact/
          else
            cp dist/ai-audio-analyzer-mcp runtime-artifact/
            chmod +x runtime-artifact/ai-audio-analyzer-mcp
          fi
          test ! -d runtime-artifact/_internal

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: runtime-artifact/
          if-no-files-found: error
          retention-days: 7
          include-hidden-files: true

  package-windows:
    name: Stage Windows beginner package
    needs: mcp-runtime
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure and build VST3
        shell: pwsh
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build --config Release --parallel 2

      - uses: actions/download-artifact@v4
        with:
          name: mcp-runtime-windows-x64
          path: input/mcp-runtime

      - name: Assemble Windows user folder
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $cmake = Get-Content CMakeLists.txt -Raw
          $match = [regex]::Match($cmake, 'project\(AIAnalyzer VERSION ([0-9.]+)')
          if (-not $match.Success) { throw 'Could not parse project version' }
          $version = $match.Groups[1].Value
          $vst3 = Get-ChildItem build/AIAnalyzer_artefacts -Recurse -Directory -Filter 'AI Audio Analyzer.vst3' | Select-Object -First 1
          if (-not $vst3) { throw 'AI Audio Analyzer.vst3 not found' }

          if (Test-Path package) { Remove-Item package -Recurse -Force }
          New-Item -ItemType Directory -Path 'package/mcp' -Force | Out-Null

          Copy-Item $vst3.FullName 'package/AI Audio Analyzer.vst3' -Recurse -Force
          Copy-Item 'input/mcp-runtime/ai-audio-analyzer-mcp.exe' 'package/mcp/ai-audio-analyzer-mcp.exe' -Force
          Copy-Item 'skills/ai-analyzer-flstudio' 'package/skill' -Recurse -Force
          Copy-Item release/common/START-HERE.md 'package/START-HERE.md'
          Copy-Item release/common/MCP-SETUP.md 'package/MCP-SETUP.md'
          Copy-Item release/common/INSTALL.en.md 'package/INSTALL.en.md'
          Copy-Item release/common/INSTALL.zh-CN.md 'package/INSTALL.zh-CN.md'
          Copy-Item release/windows/Install.ps1 'package/Install.ps1'
          Copy-Item release/windows/Install.cmd 'package/Install.cmd'
          Copy-Item LICENSE 'package/LICENSE'

          @"
          AI Audio Analyzer $version
          Platform: Windows x64
          MCP: 1.2 standalone one-file executable
          MCP tools: 48
          Guide Resources: 16
          OSC protocol: 1.2 append-only
          Analyzer control protocol: local revision 1
          Release commit: $env:GITHUB_SHA
          "@ | Set-Content 'package/VERSION.txt' -Encoding UTF8

          $null = [scriptblock]::Create((Get-Content 'package/Install.ps1' -Raw))
          $env:AI_ANALYZER_SELF_TEST = '1'
          $env:AI_ANALYZER_REQUIRE_GUIDES = '1'
          $env:AI_ANALYZER_SKILL_DIR = (Resolve-Path 'package/skill').Path
          & 'package/mcp/ai-audio-analyzer-mcp.exe'
          if ($LASTEXITCODE -ne 0) { throw 'Packaged Windows MCP failed final self-description/guide self-test' }
          Remove-Item Env:AI_ANALYZER_SELF_TEST -ErrorAction SilentlyContinue
          Remove-Item Env:AI_ANALYZER_REQUIRE_GUIDES -ErrorAction SilentlyContinue
          Remove-Item Env:AI_ANALYZER_SKILL_DIR -ErrorAction SilentlyContinue

          $forbidden = Get-ChildItem package -Recurse -File | Where-Object {
            $_.Extension -eq '.py' -or $_.Extension -eq '.zip' -or $_.Name -eq 'requirements.txt' -or $_.Name -eq 'cherry-studio.example.json'
          }
          if ($forbidden) { throw "Forbidden developer/nested archive files in user package: $($forbidden.FullName -join ', ')" }
          if (-not (Test-Path 'package/LICENSE')) { throw 'LICENSE missing from Windows user Release.' }
          if (-not (Test-Path 'package/MCP-SETUP.md')) { throw 'MCP-SETUP.md missing from Windows user Release.' }
          if (-not (Test-Path 'package/skill/SKILL.md')) { throw 'Canonical Skill missing from Windows user Release.' }
          if (Test-Path 'package/mcp/source') { throw 'mcp/source must never be present in a user Release.' }
          if (Test-Path 'package/mcp/_internal') { throw '_internal must never be present in a PyInstaller one-file user Release.' }

      - uses: actions/upload-artifact@v4
        with:
          name: AI-Audio-Analyzer-Package-Windows
          path: package/
          if-no-files-found: error
          retention-days: 7
          include-hidden-files: true

  package-macos:
    name: Stage macOS Apple Silicon beginner package
    needs: mcp-runtime
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure and build arm64 VST3
        shell: bash
        run: |
          set -euo pipefail
          test "$(uname -m)" = "arm64"
          cmake -S . -B build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=11.0 \
            -DCMAKE_OSX_ARCHITECTURES=arm64
          cmake --build build --config Release --parallel 2

      - uses: actions/download-artifact@v4
        with:
          name: mcp-runtime-macos-arm64
          path: input/mcp-runtime

      - name: Assemble macOS user folder
        shell: bash
        run: |
          set -euo pipefail
          VERSION="$(sed -nE 's/.*project\(AIAnalyzer VERSION ([0-9.]+).*/\1/p' CMakeLists.txt | head -1)"
          test -n "$VERSION"
          VST3_PATH="$(find build/AIAnalyzer_artefacts -type d -name 'AI Audio Analyzer.vst3' -print -quit)"
          test -n "$VST3_PATH"
          BINARY="$VST3_PATH/Contents/MacOS/AI Audio Analyzer"
          test "$(lipo -archs "$BINARY")" = "arm64"
          xattr -cr "$VST3_PATH" || true
          codesign --force --deep --sign - --timestamp=none "$VST3_PATH"
          codesign --verify --deep --strict --verbose=4 "$VST3_PATH"

          rm -rf package
          mkdir -p package/mcp
          ditto "$VST3_PATH" 'package/AI Audio Analyzer.vst3'
          cp input/mcp-runtime/ai-audio-analyzer-mcp package/mcp/ai-audio-analyzer-mcp
          chmod +x package/mcp/ai-audio-analyzer-mcp
          ditto skills/ai-analyzer-flstudio package/skill
          cp release/common/START-HERE.md package/START-HERE.md
          cp release/common/MCP-SETUP.md package/MCP-SETUP.md
          cp release/common/INSTALL.en.md package/INSTALL.en.md
          cp release/common/INSTALL.zh-CN.md package/INSTALL.zh-CN.md
          cp release/macos/install.sh package/install.sh
          cp release/macos/Install.command package/Install.command
          cp LICENSE package/LICENSE
          chmod +x package/install.sh package/Install.command
          AI_ANALYZER_SKILL_DIR="$PWD/package/skill" AI_ANALYZER_REQUIRE_GUIDES=1 AI_ANALYZER_SELF_TEST=1 package/mcp/ai-audio-analyzer-mcp

          cat > package/VERSION.txt <<EOF_VERSION
          AI Audio Analyzer ${VERSION}
          Platform: macOS Apple Silicon arm64
          MCP: 1.2 standalone one-file executable
          MCP tools: 48
          Guide Resources: 16
          OSC protocol: 1.2 append-only
          Analyzer control protocol: local revision 1
          Release commit: ${GITHUB_SHA}
          EOF_VERSION

          if find package -type f \( -name '*.py' -o -name '*.zip' -o -name 'requirements.txt' -o -name 'cherry-studio.example.json' \) | grep -q .; then
            echo 'Forbidden developer/nested archive file found in user package.' >&2
            exit 1
          fi
          test -s package/LICENSE
          test -s package/MCP-SETUP.md
          test -s package/skill/SKILL.md
          test ! -d package/mcp/source
          test ! -d package/mcp/_internal

      - uses: actions/upload-artifact@v4
        with:
          name: AI-Audio-Analyzer-Package-macOS
          path: package/
          if-no-files-found: error
          retention-days: 7
          include-hidden-files: true

  publish:
    name: Create single-zip GitHub Release
    needs: [package-windows, package-macos]
    runs-on: macos-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: AI-Audio-Analyzer-Package-Windows
          path: staging/windows

      - uses: actions/download-artifact@v4
        with:
          name: AI-Audio-Analyzer-Package-macOS
          path: staging/macos

      - name: Resolve release metadata
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          VERSION="$(sed -nE 's/.*project\(AIAnalyzer VERSION ([0-9.]+).*/\1/p' CMakeLists.txt | head -1)"
          test -n "$VERSION"
          TAG='${{ inputs.tag }}'
          if [ -z "$TAG" ]; then TAG="v${VERSION}"; fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Create final archives exactly once
        shell: bash
        env:
          VERSION: ${{ steps.meta.outputs.version }}
        run: |
          set -euo pipefail
          rm -rf release-assets
          mkdir -p release-assets

          WIN_ROOT="release-assets/AI-Audio-Analyzer-v${VERSION}-Windows"
          MAC_ROOT="release-assets/AI-Audio-Analyzer-v${VERSION}-macOS"
          mkdir -p "$WIN_ROOT" "$MAC_ROOT"
          ditto staging/windows "$WIN_ROOT"
          ditto staging/macos "$MAC_ROOT"

          chmod +x "$MAC_ROOT/install.sh" "$MAC_ROOT/Install.command"
          chmod +x "$MAC_ROOT/mcp/ai-audio-analyzer-mcp"
          chmod +x "$MAC_ROOT/AI Audio Analyzer.vst3/Contents/MacOS/AI Audio Analyzer"

          codesign --verify --deep --strict --verbose=4 "$MAC_ROOT/AI Audio Analyzer.vst3"
          AI_ANALYZER_SKILL_DIR="$MAC_ROOT/skill" AI_ANALYZER_REQUIRE_GUIDES=1 AI_ANALYZER_SELF_TEST=1 "$MAC_ROOT/mcp/ai-audio-analyzer-mcp"

          if find "$WIN_ROOT" "$MAC_ROOT" -type f \( -name '*.py' -o -name '*.zip' -o -name 'requirements.txt' -o -name 'cherry-studio.example.json' \) | grep -q .; then
            echo 'Final user package contains source/developer files or a nested ZIP.' >&2
            exit 1
          fi
          test -s "$WIN_ROOT/LICENSE"
          test -s "$MAC_ROOT/LICENSE"
          test -s "$WIN_ROOT/MCP-SETUP.md"
          test -s "$MAC_ROOT/MCP-SETUP.md"
          test -s "$WIN_ROOT/skill/SKILL.md"
          test -s "$MAC_ROOT/skill/SKILL.md"
          test ! -d "$WIN_ROOT/mcp/source"
          test ! -d "$MAC_ROOT/mcp/source"
          test ! -d "$WIN_ROOT/mcp/_internal"
          test ! -d "$MAC_ROOT/mcp/_internal"

          pushd release-assets >/dev/null
          zip -qry "AI-Audio-Analyzer-v${VERSION}-Windows.zip" "AI-Audio-Analyzer-v${VERSION}-Windows"
          ditto -c -k --sequesterRsrc --keepParent "AI-Audio-Analyzer-v${VERSION}-macOS" "AI-Audio-Analyzer-v${VERSION}-macOS.zip"
          rm -rf "AI-Audio-Analyzer-v${VERSION}-Windows" "AI-Audio-Analyzer-v${VERSION}-macOS"

          if unzip -Z1 "AI-Audio-Analyzer-v${VERSION}-Windows.zip" | grep -Ei '\.zip$'; then
            echo 'Windows Release ZIP contains a nested ZIP.' >&2
            exit 1
          fi
          if unzip -Z1 "AI-Audio-Analyzer-v${VERSION}-macOS.zip" | grep -Ei '\.zip$'; then
            echo 'macOS Release ZIP contains a nested ZIP.' >&2
            exit 1
          fi

          shasum -a 256 *.zip > SHA256SUMS.txt
          cat SHA256SUMS.txt
          popd >/dev/null

      - name: Build beginner-facing release notes
        shell: bash
        env:
          TAG: ${{ steps.meta.outputs.tag }}
        run: |
          cat > release-assets/RELEASE-NOTES.md <<'EOF_NOTES'
          # AI Audio Analyzer Release

          Download the package for your computer, unzip it once, and run the installer inside.

          - Windows: unzip the Windows ZIP, then double-click Install.cmd.
          - macOS Apple Silicon: unzip the macOS ZIP, then double-click Install.command.
          - No Python, source code, package manager, or programming setup is required.
          - MCP 1.2 exposes 48 tools and 16 Guide Resources. OSC analysis protocol remains append-only 1.2; Analyzer profile control remains local revision 1.
          - MCP is self-describing through Server instructions, Tool descriptions, and aianalyzer://guide/* Resources backed by the packaged Skill Markdown.
          - audio_project_identity_status() reports that stable project identity is currently unresolved; restart Analyzer MCP after changing/reopening projects when strict retained-state isolation is required.
          - Song Memory retains one-second DAW-time evidence with explicit coverage and instance-local playback epochs.
          - P4b audio_historical_detail() can query bounded one-second historical Mid/Side, stereo, mono-energy, masking and temporal-pair evidence for retained DAW ranges/Sections without forced replay when those measurements were captured.
          - P4b stores no raw audio, does not provide subsecond historical alignment, and does not provide historical mono-fold Sample Peak/True Peak.
          - P6a audio_dynamics_distribution() is descriptive retained evidence; LUFS-S spread is not standardized EBU LRA and arbitrary-range Integrated LUFS/PLR remain unavailable.
          - P7a audio_mono_compatibility() remains a recent-window direct mono energy tool. P4b historical mono energy is a separate one-second retained path.
          - P8a references remain frozen recent-window MCP-session measurement profiles; P4b does not silently make them historical or persistent.
          - A/B/C Section families are neutral recurrence labels. Relationship shortlist priority is not a problem probability or processing command.
          - Same-range verification requires actual external host readback for closed_loop_complete=true. Technical comparability does not mean a change sounds better.
          - All sound-changing/project writes remain the responsibility of the external DAW-control MCP.
          - macOS Release is Apple Silicon arm64 only and is currently ad-hoc signed rather than Apple-notarized.

          See START-HERE.md inside the package for the shortest setup path.
          EOF_NOTES

      - name: Create or update GitHub Release
        shell: bash
        env:
          TAG: ${{ steps.meta.outputs.tag }}
          PRERELEASE: ${{ inputs.prerelease }}
          DRAFT: ${{ inputs.draft }}
        run: |
          set -euo pipefail
          files=(
            "release-assets/AI-Audio-Analyzer-v${{ steps.meta.outputs.version }}-Windows.zip"
            "release-assets/AI-Audio-Analyzer-v${{ steps.meta.outputs.version }}-macOS.zip"
            "release-assets/SHA256SUMS.txt"
          )
          for file in "${files[@]}"; do test -f "$file"; done

          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "${files[@]}" --clobber
            gh release edit "$TAG" \
              --title "AI Audio Analyzer $TAG" \
              --notes-file release-assets/RELEASE-NOTES.md \
              --draft="$DRAFT" \
              --prerelease="$PRERELEASE"
          elif [ "$DRAFT" = 'true' ] && [ "$PRERELEASE" = 'true' ]; then
            gh release create "$TAG" "${files[@]}" --target "$GITHUB_SHA" --title "AI Audio Analyzer $TAG" --notes-file release-assets/RELEASE-NOTES.md --draft --prerelease
          elif [ "$DRAFT" = 'true' ]; then
            gh release create "$TAG" "${files[@]}" --target "$GITHUB_SHA" --title "AI Audio Analyzer $TAG" --notes-file release-assets/RELEASE-NOTES.md --draft
          elif [ "$PRERELEASE" = 'true' ]; then
            gh release create "$TAG" "${files[@]}" --target "$GITHUB_SHA" --title "AI Audio Analyzer $TAG" --notes-file release-assets/RELEASE-NOTES.md --prerelease
          else
            gh release create "$TAG" "${files[@]}" --target "$GITHUB_SHA" --title "AI Audio Analyzer $TAG" --notes-file release-assets/RELEASE-NOTES.md
          fi

      - name: Show release URL
        shell: bash
        env:
          TAG: ${{ steps.meta.outputs.tag }}
        run: gh release view "$TAG" --json url,isDraft,isPrerelease --jq '{url,isDraft,isPrerelease}'
